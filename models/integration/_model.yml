models:
  - name: int_claims_claimhistory
    columns:
      - name: CLAIM_SURROGATE_KEY
        tests:
          - unique
          - not_null
          - is_claimpaymentamount
  - name: int_policy_payment
    columns:
      - name: POLICY_SURROGATE_KEY
        tests:
          - unique
          - not_null
              
